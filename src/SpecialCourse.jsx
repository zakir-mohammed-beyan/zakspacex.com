const SpecialCourse = () => {
  return (
    <section className="p-20">
      <button className="relative bg-transparent p-8 border border-blue-400 overflow-hidden group">
        <span className="absolute inset-0 w-0 h-full bg-blue-400 transition-all duration-300 ease-out group-hover:w-full"></span>
        <span className="relative z-10">EXPLORE TECHNOLOGIES</span>
      </button>
    </section>
  );
};

export default SpecialCourse;
